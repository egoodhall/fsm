// generated by Textmapper; DO NOT EDIT

package parser

import (
	"strings"
	"unicode/utf8"

	"github.com/egoodhall/fsm/gen/parser/token"
)

// Lexer states.
const (
	StateInitial           = 0
	StateInFsmDefinition   = 1
	StateInTypeDeclaration = 2
	StateInOption          = 3
)

// Lexer uses a generated DFA to scan through a utf-8 encoded input string. If
// the string starts with a BOM character, it gets skipped.
type Lexer struct {
	source string

	ch          rune // current character, -1 means EOI
	offset      int  // character offset
	scanOffset  int  // scanning offset
	tokenOffset int  // last token byte offset
	line        int  // current line number (1-based)
	tokenLine   int  // last token line
	value       interface{}

	State int // lexer state, modifiable
}

var bomSeq = "\xef\xbb\xbf"

// Init prepares the lexer l to tokenize source by performing the full reset
// of the internal state.
func (l *Lexer) Init(source string) {
	l.source = source

	l.ch = 0
	l.offset = 0
	l.scanOffset = 0
	l.tokenOffset = 0
	l.line = 1
	l.tokenLine = 1
	l.State = 0

	if strings.HasPrefix(source, bomSeq) {
		l.offset += len(bomSeq)
	}

	l.rewind(l.offset)
}

// Next finds and returns the next token in l.source. The source end is
// indicated by Token.EOI.
//
// The token text can be retrieved later by calling the Text() method.
func (l *Lexer) Next() token.Type {
restart:
	l.tokenLine = l.line
	l.tokenOffset = l.offset

	state := tmStateMap[l.State]
	hash := uint32(0)
	backupRule := -1
	var backupOffset int
	backupHash := hash
	for state >= 0 {
		var ch int
		if uint(l.ch) < tmRuneClassLen {
			ch = int(tmRuneClass[l.ch])
		} else if l.ch < 0 {
			state = int(tmLexerAction[state*tmNumClasses])
			if state > tmFirstRule && state < 0 {
				state = (-1 - state) * 2
				backupRule = tmBacktracking[state]
				backupOffset = l.offset
				backupHash = hash
				state = tmBacktracking[state+1]
			}
			continue
		} else {
			ch = 1
		}
		state = int(tmLexerAction[state*tmNumClasses+ch])
		if state > tmFirstRule {
			if state < 0 {
				state = (-1 - state) * 2
				backupRule = tmBacktracking[state]
				backupOffset = l.offset
				backupHash = hash
				state = tmBacktracking[state+1]
			}
			hash = hash*uint32(31) + uint32(l.ch)
			if l.ch == '\n' {
				l.line++
			}

			// Scan the next character.
			// Note: the following code is inlined to avoid performance implications.
			l.offset = l.scanOffset
			if l.offset < len(l.source) {
				r, w := rune(l.source[l.offset]), 1
				if r >= 0x80 {
					// not ASCII
					r, w = utf8.DecodeRuneInString(l.source[l.offset:])
				}
				l.scanOffset += w
				l.ch = r
			} else {
				l.ch = -1 // EOI
			}
		}
	}

	rule := tmFirstRule - state
recovered:
	switch rule {
	case 8:
		hh := hash & 7
		switch hh {
		case 1:
			if hash == 0x68ac491 && "state" == l.source[l.tokenOffset:l.offset] {
				rule = 15
				break
			}
		case 2:
			if hash == 0x68ac462 && "start" == l.source[l.tokenOffset:l.offset] {
				rule = 14
				break
			}
		case 3:
			if hash == 0x188db && "end" == l.source[l.tokenOffset:l.offset] {
				rule = 16
				break
			}
			if hash == 0xde3 && "or" == l.source[l.tokenOffset:l.offset] {
				rule = 19
				break
			}
			if hash == 0xe7b && "to" == l.source[l.tokenOffset:l.offset] {
				rule = 18
				break
			}
		case 5:
			if hash == 0x993b6d55 && "transition" == l.source[l.tokenOffset:l.offset] {
				rule = 17
				break
			}
		}
	}

	tok := tmToken[rule]
	var space bool
	switch rule {
	case 0: // no match
		if backupRule >= 0 {
			rule = backupRule
			hash = backupHash
			l.rewind(backupOffset)
		} else if l.offset == l.tokenOffset {
			if l.ch == -1 {
				tok = token.EOI
			}
			l.rewind(l.scanOffset)
		}
		if rule != 0 {
			goto recovered
		}
	case 2: // 'fsm': /fsm/
		{
			l.State = StateInFsmDefinition
		}
	case 3: // 'type': /type/
		{
			l.State = StateInTypeDeclaration
		}
	case 4: // 'option': /option/
		{
			l.State = StateInOption
		}
	case 5: // Whitespace: /[ \t\n\r]+/
		space = true
	case 6: // EolComment: /\/\/[^\n]*\n/
		space = true
	case 7: // BlockComment: /\/\*([^*]|\*+[^*\/])*\**\*\//
		space = true
	case 12: // '}': /\}/
		{
			l.State = StateInitial
		}
	case 21: // ';': /;/, ';': /;/
		{
			l.State = StateInitial
		}
	}
	if space {
		goto restart
	}
	return tok
}

// Pos returns the start and end positions of the last token returned by Next().
func (l *Lexer) Pos() (start, end int) {
	start = l.tokenOffset
	end = l.offset
	return
}

// Line returns the line number of the last token returned by Next() (1-based).
func (l *Lexer) Line() int {
	return l.tokenLine
}

// Text returns the substring of the input corresponding to the last token.
func (l *Lexer) Text() string {
	return l.source[l.tokenOffset:l.offset]
}

// Value returns the value associated with the last returned token.
func (l *Lexer) Value() interface{} {
	return l.value
}

// Copy forks the lexer in its current state.
func (l *Lexer) Copy() Lexer {
	ret := *l
	return ret
}

// rewind can be used in lexer actions to accept a portion of a scanned token, or to include
// more text into it.
func (l *Lexer) rewind(offset int) {
	if offset < l.offset {
		l.line -= strings.Count(l.source[offset:l.offset], "\n")
	} else {
		if offset > len(l.source) {
			offset = len(l.source)
		}
		l.line += strings.Count(l.source[l.offset:offset], "\n")
	}

	// Scan the next character.
	l.scanOffset = offset
	l.offset = offset
	if l.offset < len(l.source) {
		r, w := rune(l.source[l.offset]), 1
		if r >= 0x80 {
			// not ASCII
			r, w = utf8.DecodeRuneInString(l.source[l.offset:])
		}
		l.scanOffset += w
		l.ch = r
	} else {
		l.ch = -1 // EOI
	}
}
